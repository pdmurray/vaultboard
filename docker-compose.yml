services:
  dashboard:
    image: ghcr.io/pdmurray/vaultboard:latest
    build: .
    container_name: vaultboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /mnt/vault:/vault
    environment:
      - VAULT_PATH=/vault
      - PORT=3000
